# 2026-07-30 工作记录

## 构建：极简 Markdown 转 PDF 工具站（Astro + Cloudflare Pages）

- 技术栈：Astro 4.16（static 输出），marked + DOMPurify 客户端渲染，html2pdf.js（esm.sh 动态导入）做一键下载，浏览器原生打印做「另存为 PDF」。
- 结构：
  - `src/components/MarkdownPdfTool.astro`：双栏工具（左 textarea / 右 A4 预览），工具栏含示例/清空/加粗/打印/下载。
  - `src/layouts/BaseLayout.astro`：统一注入 canonical、OG、Twitter Card、JSON-LD（WebSite / BlogPosting / SoftwareApplication）。
  - `src/content/blog/*.md`：3 篇带 SEO keywords 的示例博文（Markdown转PDF / 博客SEO / Cloudflare部署）。
  - `src/pages/sitemap.xml.ts` 与 `rss.xml.ts`：自建站点地图与 RSS（**未用 @astrojs/sitemap**，因其依赖 `astro:routes:resolved` 钩子与 Astro 4.16 不兼容，会报 `_routes.reduce` 错误）。
  - `src/site.config.ts`：集中配置 url / github，部署前必改。
- 构建验证通过：`npm run build` 成功，dist/ 含首页、博客、about、sitemap.xml、rss.xml、robots.txt。
- 已 `git init` 并提交（main 分支）。
- `npm install` 首次曾因 OOM（exit 137）中断并留下半残 node_modules，清理后重装成功。

## 推送完成（17:23 左右）
- GitHub 连接器只读（create_repository / push_files 均 403），改为用户提供 classic PAT 由本地 git 推送。
- 推送脚本：PAT 写入 `/tmp/gh_token.txt`（不回显）→ `git push https://${TOKEN}@github.com/...` → 输出 sed 脱敏 → 删除令牌 → remote 只留无令牌地址。
- 已验证：`git ls-remote --heads origin` 返回 `c6776cc... refs/heads/main`，与本地 HEAD 一致，推送成功。
- 仓库地址：`https://github.com/crazynotesman-svg/markdown-pdf-studio`
- ⚠️ 安全提醒：该 classic PAT 含 repo 权限，用完后应到 GitHub → Settings → Developer settings → Personal access tokens 撤销。

## Cloudflare Pages 部署完成 + 上线验证（17:36）
- 用户部署后反馈域名 `https://markdown-pdf-studio.pages.dev/` —— 与 `site.config.ts` / `robots.txt` 占位完全一致，**无需改配置、无需重新推送**。
- 在线探测（curl）确认全链路健康：
  - `/` 200(text/html)、`/sitemap.xml` 200(application/xml)、`/robots.txt` 200、`/rss.xml` 200
  - `/blog` `/about` 返回 308，落地 `/blog/` `/about/` 均 200（Astro trailing-slash 重定向，正常）
  - 首页含工具本体：1 个 textarea（Markdown 输入）、A4 预览字样、按钮「下载 PDF / 另存为 PDF / 打印 / 载入示例」
  - canonical / og:title / og:url / title / description meta 均正确注入
  - 文章详情 `/blog/markdown-to-pdf-guide/` 200
- 结论：极简 Markdown→PDF 工具站（Astro + Cloudflare Pages）已端到端交付，原始需求全部满足。
- 后续可选：向 Google Search Console 提交 sitemap.xml 加速收录；绑定自定义域名；撤销 classic PAT。

## 多语言 + 联系方式升级（17:45 起）
- 需求：默认英语，顶部语言切换器，支持 en/es/zh-CN/zh-TW/pt/fr/de + 另加 ja/ru/ko（共 10 种）；新增联系方式 babong@163.com。
- 方案：客户端 i18n（非 Astro 路由级多语言，避免重写整站路由与博客内容翻译）。
  - 新增 `src/i18n/translations.ts`：LANGS、DEFAULT_LANG='en'、translations（10 语言全量 UI 文案 + 每种语言一份 tool.sample 示例 Markdown）。
  - 新增 `src/scripts/i18n.ts`：applyLang 扫描 [data-i18n]（支持 {year} 替换）与 [data-i18n-attr]（标题/placeholder/aria-label），更新 html lang、select 值，localStorage 持久化，派发 langchange 事件。
  - Header 加 `#lang-select`（选项来自 LANGS）；Footer 加 mailto 联系方式；BaseLayout 引入 i18n 脚本、<html lang="en">。
  - MarkdownPdfTool：所有按钮/标签/标题 data-i18n；打印/下载按钮拆出文本 span（图标不被翻译覆盖）；示例 Markdown 按当前语言读取，切换语言时若仍是示例则同步。
  - index/about/blog 页面静态文案改英文默认 + data-i18n；about 增加联系 section；首页 tag-row 在 langchange 时按语言重建（home.keywords）。
  - site.config：locale='en'，新增 email。
- `npm run build` 通过；dist 验证：lang-select 10 选项、data-i18n 属性、字典已打包（含 ja/es/zh-TW 示例）、babong@163.com 出现在首页与 about。
- 已 `git add src && git commit`（main 本地）。`git push` 失败：osxkeychain 无凭据、终端提示禁用；GitHub 连接器只读。需用户提供新 PAT 或自行 push。
- 已知限制：博客 3 篇文章正文仍为中文（未做全文翻译）；<title>/meta description 为英文默认（客户端切换不影响 head）。

## 部署要点（写入 README.md）
- Cloudflare Pages：Connect to Git → 构建命令 `npm run build`、输出 `dist`；改 `site.config.ts` 的 url 与 robots.txt 的 Sitemap 地址。

## GitHub 连接器连通性测试（16:52）
- 连接器状态：connected。调用只读 `mcp__github__get_me` 成功，确认已鉴权。
- 认证账号：`crazynotesman-svg`（id 298115288，6 个公开仓库，2026-06-30 创建）。
- 可用能力含 `create_repository` / `push_files` / `get_file_contents` 等，足以把本项目推到该账号并完成 Cloudflare Pages 关联。
- ⚠️ 实测 `mcp__github__create_repository` 返回 **403 Resource not accessible by integration**：该连接器为受限 GitHub App/令牌，可读取但**不能创建仓库**。
- ⚠️ 进一步实测 `mcp__github__push_files`（往已存在的空仓库 crazynotesman-svg/markdown-pdf-studio 写文件）同样 **403**。结论：**该 GitHub 连接器是只读的**，无法建仓库也无法推送。
- 因此「关联 GitHub」只能靠本地 git：用户手动 `git push`，或提供 classic PAT（repo 权限）由我用本地 git 推送。环境里无 GITHUB_TOKEN 可用。
- 已 `git commit` 最新代码（site.config.ts 用真实仓库地址、博客占位链接已修正），本地 main 分支就绪，等推送。
